#include <stdio.h>

/**
 * main - prints the phrase with proper grammar, but the outcome is a piece of art,
 * Return: 0 if no error encountred
 */

int main(void)
{
	printf("with proper grammar,");
	printf(" but the outcome is a");
	printf(" piece of art,\n");
	return (0);
}
